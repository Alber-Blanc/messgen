name: messgen_tests
description: Tests for Messgen Dart port
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  typed_data: ^1.3.2
  messgen:
    path: ../../port/dart

dev_dependencies:
  test: ^1.24.0
